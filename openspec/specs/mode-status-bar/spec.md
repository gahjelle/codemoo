# Spec: mode-status-bar

## Purpose

TBD — Defines how the active mode name is displayed in the `BackendStatus` widget's left section.

## Requirements

> **Note**: All requirements in this spec have been removed as part of the `remove-mode-concept` change. Mode display in the status bar is superseded by bot/variant display in the `backend-status-bar` spec.
