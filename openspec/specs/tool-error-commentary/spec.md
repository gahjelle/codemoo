# Spec: tool-error-commentary

## Purpose

REMOVED — All requirements from this spec have been migrated to `commentary-events` as part of the `consolidate-commentator-events` change. `ToolErrorEvent` is replaced by `ToolEvent(outcome="error")`. See `commentary-events` spec for the current requirements governing tool error commentary.

## Requirements
